import React, { Component } from 'react';
import styles from '../coin_card/CoinCard.css';

class CoinCard extends Component {
  render(){
    return(
      <div className={styles.coin_container}>
      </div>
    )
  }
}

export default CoinCard;
